namespace Editor {
  class App {
    const _platform UI.Platform

    def new(platform UI.Platform) {
      _platform = platform
      createWindow
    }

    def createWindow {
      WindowController.new(_platform)
    }
  }
}
